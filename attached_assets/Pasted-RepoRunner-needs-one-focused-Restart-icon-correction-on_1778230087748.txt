RepoRunner needs one **focused Restart icon correction** only.

Do **not** redesign the UI.
Do **not** change layout.
Do **not** change button labels.
Do **not** change button sizes.
Do **not** change button styling.
Do **not** change any icons except the `Restart` icon.
Do **not** change engine lights, logs, meteors, setup modal, header, IPC, process logic, or product flow.

This task is only about correcting the custom `Restart` icon.

## Important

I am also pasting the reference image into this chat.

Use that reference image directly.

Do **not** interpret loosely.
Do **not** improvise a different symbol.
Do **not** approximate it with generic curves.
Match the reference shape closely.

## Current issue

The current `Restart` icon is wrong.

It does **not** match the reference image closely enough.

The reference image reads like:

* two smooth sine-wave style curves
* mirrored left/right
* crossing through the middle
* balanced and symmetrical
* like two interwoven wave lines

The current version does not capture that properly.

## Correct visual direction

The `Restart` icon should look like:

* two mirrored sine-wave curves
* crossing through the center
* smooth and elegant
* symmetrical
* compact and readable at button size
* futuristic / technical / minimal

It should feel like:

```text
two crossing wave paths
```

not like:

* an X
* a generic twist
* random bezier curves
* a DNA ladder
* a stretched bowtie
* a small scribble

## Build requirement

Replace the current `Restart` icon geometry with a custom SVG that is deliberately drawn to match the reference image.

Use:

* stroke only
* no fill
* `currentColor`
* rounded stroke caps
* rounded stroke joins
* same shared icon size system as the other button icons
* same visual stroke weight as the other icons

## Shape requirement

Build the icon from **two mirrored curved paths** only:

1. one curve bowing inward then outward
2. the other mirrored against it

These two curves should cross in the center and create the exact visual impression of the reference image.

The icon should be:

* optically centered
* clearly legible
* not too small
* not too thin
* not too tall
* not too wide

## Very important instruction

Do **not** just tweak the existing icon slightly.

If necessary, fully replace the path data.

This needs to be a **clean redraw** so the result actually matches the reference.

## Scope restraint

Only update the `Restart` icon.

Do not touch:

* Pull icon
* Install icon
* Open icon
* Stop Engine icon
* Start icons
* button layout
* text spacing
* button materials
* any other UI

## Acceptance criteria

After this change:

1. The `Restart` icon clearly reads as two crossing sine-wave style curves.
2. It closely matches the pasted reference image.
3. It feels symmetrical and balanced.
4. It is readable at normal button size.
5. It visually matches the weight and polish of the other custom icons.
6. No unrelated UI or functionality changes are made.
