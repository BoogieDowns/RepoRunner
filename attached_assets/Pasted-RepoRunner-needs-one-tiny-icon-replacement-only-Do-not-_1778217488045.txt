RepoRunner needs one **tiny icon replacement** only.

Do **not** redesign the UI.
Do **not** change layout.
Do **not** change functionality.
Do **not** change button labels, button sizing, button styling, engine lights, logs, meteors, setup modal, IPC, process logic, or product flow.

This is only about replacing the **Stop Engine button icon**.

## Requested change

The `Stop Engine` button currently uses a square stop icon.

Replace that icon with the circular crossed symbol shown in the reference image:

```text
a circle with an X/cross inside it
```

It should look like the uploaded/reference symbol.

## Important

This symbol should be available in the LunaObscura font file here:

```text
artifacts/reporunner/src/assets/fonts/hs_lunaobscura Font
```

Please inspect the font / existing icon usage and use the correct glyph from that font if available.

Do not guess randomly if there is already a matching glyph in the font.

## Scope

Apply this only to:

```text
Stop Engine
```

Do not change the icons for:

```text
Start Frontend
Start Backend
Pull
Install
Restart
Open
```

## Visual requirements

The new Stop Engine icon should:

* match the style of the reference symbol
* sit in the same icon position as the current square icon
* be vertically centered with the text
* be visually balanced with the LunaObscura button font
* use the current button icon color/styling
* not make the button wider or taller
* not alter button spacing

## Preserve everything else

Do not change:

* Stop Engine text
* Stop Engine click behavior
* disabled/enabled state behavior
* red glass button styling
* layout
* any other app behavior

## Acceptance criteria

After this change:

1. The `Stop Engine` button no longer uses the square stop icon.
2. It uses the circle-with-X symbol from the reference.
3. The icon appears clean, centered, and properly sized.
4. No other button icons are changed.
5. No layout or functionality changes are made.
