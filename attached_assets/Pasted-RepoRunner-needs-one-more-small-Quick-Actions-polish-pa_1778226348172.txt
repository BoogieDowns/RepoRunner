RepoRunner needs one more **small Quick Actions polish pass** only.

Do **not** redesign the UI.
Do **not** change layout.
Do **not** change button order.
Do **not** change button labels.
Do **not** change icon SVG geometry.
Do **not** change icon size.
Do **not** change functionality.
Do **not** change engine lights, logs, meteors, setup modal, header, IPC, process logic, or product flow.

This is only about final visual refinement of the Quick Actions buttons.

## Current state

The last pass smoothed the body gradients and slightly improved neutral text readability.

Good.

Now please focus on **overall button finish at the full panel level**.

If the preview is showing the setup screen and not the dashboard, do not guess blindly. Either use the existing code carefully or switch/load whatever mock state is needed to visually inspect the Quick Actions panel before making changes.

## Goal

Make the Quick Actions buttons feel slightly more finished and cohesive without changing their structure.

They should feel:

* premium
* readable
* dimensional
* physically glassy
* clean
* not overworked

## 1. Check real dashboard view

Before editing, visually inspect the dashboard Quick Actions panel if possible.

Do not polish from memory only if the dashboard can be shown.

Check:

* red button finish
* neutral button finish
* disabled button finish
* icon/text readability
* row balance
* button material consistency

## 2. Improve icon/text contrast only if needed

If icons or labels still feel slightly buried inside the glass, lift them very slightly.

Do not make text/icons white-hot.

Target:

```text
3–6% readability improvement only if needed
```

Keep the dark premium mood.

## 3. Refine disabled state if needed

Disabled buttons should be clearly inactive but still readable as physical controls.

If disabled buttons are too invisible, increase opacity slightly.
If they look too clickable, reduce emphasis slightly.

Do not change disabled logic.

## 4. Refine panel-level cohesion

Make sure the Quick Actions card feels like a single designed module.

Check:

* button row spacing
* button glow strength
* card edge vs button edge separation
* visual weight of top row vs bottom row
* neutral buttons vs red buttons

Only make tiny adjustments.

## 5. Avoid more heavy material changes

Do not keep adding more shadow layers or complex gradients unless there is a clear visual problem.

The material is already close.

This pass should be about **balance**, not adding effects.

## 6. Stop if it already looks good

If after inspecting the dashboard the buttons already look good, make no unnecessary changes.

Do not change code just to show activity.

## Acceptance criteria

After this pass:

1. Quick Actions buttons look slightly more balanced and finished.
2. Icons and labels remain readable.
3. Disabled states remain clear.
4. Red and neutral buttons feel cohesive.
5. No layout, icon geometry, labels, or functionality are changed.
6. No unnecessary changes are made if the current state is already good.
