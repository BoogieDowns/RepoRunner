Do one focused fix only on the **Open** button icon.

Do **not** change:
- any other button icons
- button layout
- button sizes
- labels
- spacing
- typography
- glass styling
- panel layout
- anything else in the UI

This is only about fixing the **Open** icon.

## Problem

The Open icon is still messy.

The main issue is:
- one of the diagonal straight lines has a visible gap before it reaches the circle
- the geometry still feels slightly awkward and inconsistent
- it does not yet feel as clean and manufactured as the better icons

## Required fix

The Open icon should be rebuilt / corrected so it looks like a clean, premium symbol from the same icon family as the other good icons.

### The icon should be:

- a circle in the upper-right
- three clean diagonal strokes trailing down-left
- all strokes parallel
- all strokes clean and deliberate
- properly centered
- optically balanced
- consistent in stroke thickness with the other icons

## Important geometry rules

Please follow these strictly:

1. The **top short stroke** must visually connect cleanly into the circle area.
   - there should be **no awkward gap**
   - it should feel intentional and clean
   - do not leave it floating

2. The **middle and lower strokes** should:
   - be parallel
   - be evenly spaced
   - have clean lengths
   - trail down-left in a tidy, symmetrical way

3. The **circle** should:
   - be slightly more dominant
   - sit clearly in the upper-right
   - feel like the “head” of the symbol
   - be crisp and clean at button size

4. The icon should feel:
   - sharper
   - cleaner
   - more premium
   - more readable
   - more like it came from the same factory as the other icons

## Implementation guidance

If the current SVG is messy, **replace it instead of nudging it again**.

Do not just make tiny coordinate changes and stop.

Rebuild the Open icon cleanly if needed.

## Most important instruction

Judge it by how it looks **inside the actual Open button**, not by whether the coordinates seem mathematically reasonable.

Do not finish until:
- the gap issue is gone
- the icon looks clean
- the linework feels intentional
- the icon matches the system better

## Acceptance criteria

This is only successful if:

1. the gap before the circle is gone
2. the three diagonal strokes read clearly
3. the circle is clean and dominant
4. the icon looks more polished and intentional
5. the icon feels consistent with the rest of the button icon set
6. nothing else in the UI is changed