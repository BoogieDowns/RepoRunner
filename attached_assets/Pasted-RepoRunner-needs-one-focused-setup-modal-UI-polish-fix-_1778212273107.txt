RepoRunner needs one **focused setup modal UI polish fix** only.

Do **not** redesign the app.
Do **not** change layout structure.
Do **not** change functionality.
Do **not** change setup behavior, save behavior, folder picker behavior, IPC, process logic, logs, meteors, engine lights, or dashboard buttons.

This is only about two small visual/text changes in the **Setup modal**.

## 1. Change setup modal heading wording

In the setup modal, change the heading from:

```text
Setup RepoRunner
```

to:

```text
RepoRunner Setup
```

Only change the visible heading text.

Do not change any behavior.

## 2. Restyle the Save Configuration button

The large red button at the bottom of the setup modal currently does not match the newer red action buttons on the main dashboard.

Please restyle the `Save Configuration` button so it matches the same visual language as the main red action buttons.

It should use:

* the same glossy glass / reflective red button material
* the same LunaObscura-style button font
* the same sharper sci-fi control-panel feeling
* similar red gradient / highlight treatment
* similar border/glow treatment
* similar icon/text alignment quality

It should still be a wide primary modal button, but the material/style should match the main red buttons.

## Important

Do **not** change:

* button text
* save behavior
* click handler
* icon unless needed for visual alignment
* modal fields
* modal spacing except tiny alignment changes needed for the button
* dashboard button layout
* app functionality

## Desired result

After this change:

* the modal heading reads `RepoRunner Setup`
* the save button feels like part of the same button system as the dashboard actions
* the button no longer looks like a generic flat red web button
* the modal still feels clean and premium

## Acceptance criteria

1. Heading says `RepoRunner Setup`.
2. Save button uses the same font style as the action buttons.
3. Save button looks glossy/glassy like the red action buttons.
4. Save button remains full-width / modal-primary as it is now.
5. Save functionality is unchanged.
6. No unrelated UI or functionality changes are made.
