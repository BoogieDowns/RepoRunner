RepoRunner is looking good. Please make one small color styling refinement only.

Do **not** redesign the UI.
Do **not** change layout.
Do **not** change app functionality.
Do **not** change process management, logs behavior, setup behavior, IPC, product flow, or meteor behavior.

## Requested change

Change the current orange styling used for transitional/install states to a more premium **molten amber** color.

This applies only to:

1. **Starting / Stopping status pills**
2. **`[INSTALL]` source labels in the logs/terminal section**

## Color direction

Use a richer amber/gold tone instead of plain orange.

Suggested tokens:

```css
--transition: #ffb000;
--transition-bg: rgba(255, 176, 0, 0.10);
--transition-border: rgba(255, 176, 0, 0.42);
--transition-glow: rgba(255, 176, 0, 0.18);
```

This should feel:

* premium
* readable
* compatible with the black/red theme
* warmer than red but less basic than orange
* clearly distinct from Running and Failed states

## 1. Starting / Stopping pills

Any service status pill showing:

```text
Starting
Stopping
```

should use the molten amber treatment.

Apply the amber color to:

* text
* border
* small dot/accent if present
* subtle background tint if present
* any glow associated with that transitional state

## 2. `[INSTALL]` log labels

In the logs/terminal section, change the `[INSTALL]` source label color to the same molten amber.

Only change the `[INSTALL]` source label color.
Do not change the log message text.

## Preserve everything else

Do not change:

* Running pill color
* Stopped pill color
* Failed pill color
* Unknown pill color
* Frontend log label color
* Backend log label color
* System log label color
* Git log label color
* button styling
* meteor styling
* card styling
* modal styling
* layout
* functionality

## Acceptance criteria

After this change:

1. `Starting` pills use the molten amber styling.
2. `Stopping` pills use the molten amber styling.
3. `[INSTALL]` log labels use the same molten amber styling.
4. The color feels more premium than plain orange.
5. No layout or functionality changes are made.
