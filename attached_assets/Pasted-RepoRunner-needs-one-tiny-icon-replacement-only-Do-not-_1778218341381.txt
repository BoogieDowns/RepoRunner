RepoRunner needs one **tiny icon replacement** only.

Do **not** redesign the UI.
Do **not** change layout.
Do **not** change functionality.
Do **not** change button labels, button sizing, button styling, engine lights, logs, meteors, setup modal, IPC, process logic, or product flow.

This is only about replacing the **Pull button icon**.

## Requested change

Replace the current `Pull` button icon with a custom icon matching the uploaded/reference symbol.

The reference looks like:

```text
three stacked curved oval / ripple-like rings
```

It should feel like a stylized pull/download/signal/ripple symbol.

## Important

Do not use a generic icon library icon if it does not match the reference.

Use a **custom SVG icon** if needed, the same way the Stop Engine circle-X icon was implemented.

## Scope

Apply this only to:

```text
Pull
```

Do not change icons for:

```text
Start Frontend
Start Backend
Stop Engine
Install
Restart
Open
```

## Visual requirements

The new Pull icon should:

* match the reference shape as closely as practical
* use `currentColor`
* match the stroke weight of the other button icons
* sit in the same icon position as the current Pull icon
* be vertically centered with the text
* be visually balanced with the LunaObscura button font
* work in enabled/disabled states
* not resize the button
* not alter button spacing

## Implementation direction

Create a small custom SVG component if needed.

Suggested style:

* no fill, or minimal fill only if required
* thin curved strokes
* rounded stroke caps/joins
* stacked oval/ring form
* same visual weight as existing button icons

## Preserve everything else

Do not change:

* Pull text
* Pull click behavior
* Pull functionality
* button styling
* layout
* any other app behavior

## Acceptance criteria

After this change:

1. The `Pull` button no longer uses the old download icon.
2. It uses a custom symbol matching the stacked curved oval/ripple reference.
3. The icon is clean, centered, and properly sized.
4. No other button icons are changed.
5. No layout or functionality changes are made.
