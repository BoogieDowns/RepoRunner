RepoRunner needs one **layout refinement pass** on the Setup modal only.

Do **not** redesign the modal.
Do **not** change functionality.
Do **not** change validation.
Do **not** change field labels or button behavior.
Do **not** change the visual style direction.
Do **not** touch dashboard, buttons, engine lights, logs, meteors, or header branding.

This is only about improving the **field layout and spacing** in the Setup modal.

## Goal

Fix the awkward spacing around the `Preview URL` field and give that field more room, since URLs can be longer and visually need a wider input.

## Required layout change

Please restructure the modal form layout into this order:

### Row 1
- `Project Name`
- `Local Repository Folder`
- `Choose` button remains attached to the folder input as it is now

### Row 2
- `Preview URL`

This `Preview URL` field should span **almost the full usable modal width** (a full wide row), not sit cramped beside the port inputs.

### Row 3
- `Install Command`
- `Frontend Command`
- `Backend Command`

### Row 4
- `Frontend Port`
- `Backend Port`

These two port fields should sit together on the final row and remain narrower, since they only contain short numeric values.

## Important layout guidance

1. The `Preview URL` input should be noticeably wider than it is now.
2. The `Preview URL` row should feel clean, centered, and intentional.
3. The two port fields should no longer share the row with `Preview URL`.
4. Keep spacing even and visually balanced between all rows.
5. Keep the existing modal width unless a very small increase is needed for balance.
6. Maintain the same overall visual style, input styling, typography, and button styling.

## Visual intent

The form should feel:

- cleaner
- more orderly
- more spacious
- more visually logical

The key idea is:

- long-value field (`Preview URL`) gets a wide row
- short-value fields (`Frontend Port`, `Backend Port`) get the compact row

## Acceptance criteria

After this change:

1. `Preview URL` is moved up into its own wide row.
2. `Preview URL` is significantly wider and better suited for longer URLs.
3. `Frontend Port` and `Backend Port` are grouped together on the bottom row.
4. Row spacing feels balanced and clean.
5. No functionality or styling direction is changed beyond this layout refinement.

Please make this a **clean structural layout pass only**, not a redesign.