RepoRunner needs one **focused Setup modal polish pass** only.

Do **not** redesign the app.
Do **not** change functionality.
Do **not** change setup behavior.
Do **not** change saved config behavior.
Do **not** change folder picker behavior.
Do **not** change dashboard buttons, engine lights, logs, meteors, header, IPC, process logic, or product flow.

This is only about making the **RepoRunner Setup modal** feel as polished as the dashboard.

## Current goal

The dashboard Quick Actions buttons are now done.

Next, polish the Setup modal so it feels like part of the same premium black/red glass-control interface.

## Scope

Only inspect and polish:

* modal heading
* input fields
* labels
* field spacing
* form grid alignment
* close X
* Save Configuration button
* modal border/shadow
* overlay/scrim if needed

## Do not change

Do not add fields.
Do not remove fields.
Do not rename fields.
Do not change validation.
Do not change save behavior.
Do not change close behavior.
Do not re-add helper text under every field.

## 1. Inspect the modal visually first

Open the Setup modal and inspect it directly.

Do not guess from code only.

Check:

* heading alignment
* form field spacing
* input readability
* label readability
* path/URL field width
* close X alignment
* Save Configuration button consistency
* modal edge/border polish
* overall balance

## 2. Make the modal match the dashboard quality

The modal should feel:

* premium
* dark
* clean
* technical
* readable
* aligned with the black/red sci-fi console style
* not like a generic form pasted on top

## 3. Input field polish

Check input fields for:

* consistent height
* consistent border radius
* readable text color
* readable placeholder color
* good focus state
* clean border/glow
* no cramped text
* good internal padding

The long fields should remain comfortable:

* Local Repository Folder
* Preview URL

## 4. Label polish

Labels should be:

* readable
* aligned
* consistent
* not too bright
* not too dim
* visually connected to their input

Do not add extra helper copy.

## 5. Save Configuration button

The Save Configuration button should match the completed Quick Actions red glass button language.

Check that it has:

* same LunaObscura-style font
* same red glass material quality
* same premium highlight/depth feel
* good full-width primary modal-button treatment

Do not change its functionality.

## 6. Close X polish

The close X should feel intentional and aligned.

Check:

* size
* position
* hover state
* color
* hit area
* visual balance with heading

Do not remove it.

## 7. Modal shell polish

Check:

* modal padding
* border definition
* shadow/elevation
* subtle red edge/glow
* overlay/scrim balance
* corner radius consistency

Keep it restrained.

## 8. Only fix visible issues

If something already looks good, leave it alone.

Do not make unnecessary changes.

## Acceptance criteria

After this pass:

1. Setup modal feels visually consistent with the dashboard.
2. Inputs are readable and evenly aligned.
3. Labels are clean and consistent.
4. Save Configuration button matches the red glass button system.
5. Close X feels polished and aligned.
6. Modal shell feels premium and integrated.
7. No setup functionality is changed.
8. No unrelated UI or functionality changes are made.
