RepoRunner’s `[INSTALL]` label and Starting/Stopping pill color still looks too bright and too close to the original orange/yellow.

Please do one very small color correction only.

Do **not** redesign the UI.
Do **not** change layout.
Do **not** change functionality.
Do **not** change meteor behavior.
Do **not** change logs behavior except the `[INSTALL]` label color.
Do **not** change status logic.

## Problem

The current install/transitional color still appears as bright yellow-orange in the UI.

I want it to be more muted, darker, and bronze-like.

It should feel like:

* aged amber
* muted bronze
* subdued gold
* less saturated
* less bright
* more premium

## Required color

Please use this darker bronze/amber color:

```css
--transition: #b8792a;
--transition-bg: rgba(184, 121, 42, 0.10);
--transition-border: rgba(184, 121, 42, 0.40);
--transition-glow: rgba(184, 121, 42, 0.14);
```

## Important implementation check

Please search for any hardcoded orange/yellow values still being used for install/transitional styling.

Look for values like:

```css
#ff9f1c
#ffb000
#f59e0b
#fbbf24
orange
yellow
amber
```

If any of these are still used for:

* `[INSTALL]` labels
* `Starting` pills
* `Stopping` pills

replace them with the new `#b8792a` token family.

## Apply only to

### 1. `[INSTALL]` log source labels

The `[INSTALL]` label in the logs/terminal section should use:

```css
#b8792a
```

It should look muted bronze/amber, not bright orange/yellow.

### 2. Starting / Stopping status pills

Any service status pill showing:

```text
Starting
Stopping
```

should use the same muted bronze/amber treatment.

Apply it to:

* pill text
* border
* dot/accent if present
* subtle background tint
* subtle glow if present

## Preserve everything else

Do not change:

* Running pill color
* Stopped pill color
* Failed pill color
* Unknown pill color
* Frontend log label color
* Backend log label color
* System log label color
* Git log label color
* button styling
* meteor styling
* card styling
* modal styling
* layout
* functionality

## Acceptance criteria

After this change:

1. `[INSTALL]` labels look muted bronze/amber, not bright orange/yellow.
2. `Starting` pills use the same muted bronze/amber.
3. `Stopping` pills use the same muted bronze/amber.
4. There are no remaining hardcoded bright orange/yellow values controlling these elements.
5. The color is visibly closer to `#b8792a`.
6. No other styling or functionality changes are made.
