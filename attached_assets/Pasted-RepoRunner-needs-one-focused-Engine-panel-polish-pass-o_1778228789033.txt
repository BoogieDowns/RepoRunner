RepoRunner needs one **focused Engine panel polish pass** only.

Do **not** redesign the app.
Do **not** change functionality.
Do **not** change engine/service logic.
Do **not** change start/stop behavior.
Do **not** change IPC, process management, logs behavior, setup behavior, meteors, header, Quick Actions buttons, or product flow.

This is only about making the **Engine panel** visually match the polish level of the Quick Actions card.

## Current goal

The Quick Actions buttons are now polished.
The red brand color is now a richer crimson and looks good.

Next, inspect and polish the Engine panel so it feels equally finished.

## Scope

Only inspect and polish:

* Engine panel card
* `ENGINE` section label
* Frontend row
* Backend row
* port text
* status light placement
* status light visual integration
* row divider
* card border/shadow/background

## Do not change

Do not change:

* status logic
* status wording
* status timing
* status light animation behavior
* start/stop functionality
* port values
* service names
* layout outside the Engine panel

## 1. Inspect visually first

Open the dashboard and inspect the Engine panel directly.

Check it beside the Quick Actions card.

The Engine panel should feel like it belongs to the same polished control system.

## 2. Card shell polish

Check:

* border definition
* background depth
* subtle inner shadow
* card edge clarity
* consistency with Quick Actions card

The card should feel clean and premium, not flat.

Do not make it brighter overall.

## 3. Engine label polish

The `ENGINE` label should feel consistent with `QUICK ACTIONS`.

Check:

* letter spacing
* color
* opacity
* position
* vertical rhythm

Do not rename it.

## 4. Row layout polish

For the Frontend and Backend rows, check:

* label alignment
* port alignment
* spacing between service text and status light
* row divider clarity
* overall breathing room

The rows should feel balanced and intentional.

## 5. Status light integration

The status lights already have their behavior mostly sorted.

Do not change the behavior.

Only check visual integration:

* size relative to row
* alignment with service labels
* spacing from right edge
* balance inside the row
* whether the light feels attached to the row rather than floating

Do not rework animation logic.

## 6. Port text polish

The port text like:

```text
:3000
:3001
```

should remain secondary but readable.

Check whether it is too dim or too disconnected from the service label.

Make a tiny adjustment only if needed.

## 7. Preserve current crimson system

If any red in the Engine panel needs retinting, use the current crimson brand family.

Do not introduce a new red.

## 8. Only fix visible issues

If something already looks good, leave it alone.

Do not make changes just to make changes.

## Acceptance criteria

After this pass:

1. Engine panel visually matches the polish level of the Quick Actions card.
2. Frontend and Backend rows feel balanced.
3. Status lights are cleanly aligned and integrated.
4. Port text is secondary but readable.
5. Card shell feels premium and consistent.
6. No status behavior or app functionality changes are made.
