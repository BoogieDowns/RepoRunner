RepoRunner needs one **tiny icon replacement** only.

Do **not** redesign the UI.
Do **not** change layout.
Do **not** change functionality.
Do **not** change button labels, button sizing, button styling, engine lights, logs, meteors, setup modal, IPC, process logic, or product flow.

This is only about replacing the **Restart** button icon.

## Requested change

Replace the current `Restart` button icon with a custom icon matching the uploaded/reference symbol.

## What the symbol should look like

The reference symbol reads like:

* a **small central oval / lens / ring**
* with **two mirrored curved strokes**
* one stroke sweeping in from the left and one from the right
* the strokes cross through the center so the whole symbol feels like a
  **symmetrical orbital / mirrored X-curve / sci-fi loop mark**

A good mental model is:

```text
two mirrored flowing curves
crossing through a small central oval
```

It should feel:

* clean
* minimal
* futuristic
* symmetrical
* consistent with the other custom button icons

## Important

Do not use a generic icon library icon if it does not closely match the reference.

Use a **custom SVG icon** if needed, the same way the Stop Engine, Pull, Open, and Install icons were handled.

Do **not** waste time trying to pull this from the font unless you can explicitly verify the exact glyph exists and matches the reference.
If not, build the SVG directly.

## Scope

Apply this only to:

```text
Restart
```

Do not change icons for:

```text
Start Frontend
Start Backend
Stop Engine
Pull
Install
Open
```

## Visual requirements

The new Restart icon should:

* match the uploaded reference as closely as practical
* be horizontally symmetrical
* include a small central oval/ring
* include two mirrored crossing curves
* use `currentColor`
* use no fill unless absolutely needed
* match the stroke weight of the other button icons
* sit in the same icon position as the current Restart icon
* be vertically centered with the text
* be visually balanced with the LunaObscura button font
* work in enabled and disabled states
* not resize the button
* not alter button spacing

## Implementation direction

Create a small custom SVG component.

Suggested direction:

* central small oval/ring
* one mirrored curve on the left
* one mirrored curve on the right
* the two curves cross through the center
* rounded stroke caps / joins
* same visual weight as the other custom icons

If helpful, think of it as:

```text
a compact mirrored sci-fi “X-orbit” symbol
```

## Preserve everything else

Do not change:

* Restart text
* Restart click behavior
* Restart functionality
* button styling
* layout
* any other app behavior

## Acceptance criteria

After this change:

1. The `Restart` button no longer uses the old icon.
2. It uses a custom symbol matching the mirrored curved reference.
3. The icon is clean, centered, and properly sized.
4. No other button icons are changed.
5. No layout or functionality changes are made.
