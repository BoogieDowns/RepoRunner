RepoRunner is looking good. Please make one small service-status styling change only.

Do **not** redesign the UI.
Do **not** change layout.
Do **not** change functionality.
Do **not** change service logic, process management, IPC, setup behavior, logs behavior, or meteor behavior.

## Requested change

Only when a service status pill says:

```text
Running
```

I want that pill to have a **gentle red pulse**.

Apply this only to the two running pills in the Services card:

* Frontend → Running
* Backend → Running

## Desired effect

The Running pill should feel:

* alive
* active
* subtly energized
* premium
* soft

I do **not** want:

* flashing
* harsh blinking
* a fast pulse
* a distracting neon effect
* a large glow explosion

## Visual direction

The pulse should be:

* slow
* subtle
* red
* smooth
* elegant

A good direction would be:

* a faint rhythmic outer glow
* a very slight change in red intensity
* maybe a tiny soft pulse in the dot and/or border glow

## Scope

Apply this **only** when the pill state is `Running`.

Do **not** pulse:

* Stopped
* Starting
* Stopping
* Failed
* Unknown

## Implementation guidance

A subtle CSS animation is fine.

For example, pulse one or more of:

* box-shadow
* border glow
* background tint intensity
* dot glow

Keep the movement restrained.

Suggested timing direction:

* around `2s` to `3s`
* `ease-in-out`
* infinite
* smooth

## Preserve readability

Make sure:

* the `Running` text stays readable
* the pill still looks clean
* the Services card does not become visually noisy

## Preserve everything else

Do not change:

* pill shape
* pill size
* pill text
* layout
* cards
* logs
* buttons
* modal
* meteor effect
* functionality

## Acceptance criteria

After this change:

1. `Running` pills gently pulse red.
2. The effect is subtle and smooth.
3. Only `Running` pills pulse.
4. Other status pills remain static.
5. No layout or functionality changes are made.
