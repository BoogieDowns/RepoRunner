The top-left imported RepoRunner branding is still far too small.

Please do one **strict header sizing correction pass** only.

Do **not** redesign the app.
Do **not** change functionality.
Do **not** change buttons, engine lights, logs, meteors, setup modal, dashboard structure, IPC, or product flow.

This is only about the **top-left imported logo mark + wordmark size and header balance**.

## Core issue

The current result is still under-scaled.

It is **nowhere near big enough**.

The imported logo mark and wordmark should be approximately **2x the current visual size**.

## Required changes

### 1. Make the logo mark much larger

Increase the imported logo mark to about **double its current rendered size**.

It should feel clearly visible and intentional.

Keep it:

* crisp
* proportionally scaled
* not stretched
* not blurry
* vertically centered in the header

### 2. Make the wordmark much larger

Increase the imported `REPORUNNER` wordmark to about **double its current rendered size**.

It should be clearly readable at a glance and feel like a proper primary brand asset.

Keep it:

* crisp
* proportionally scaled
* not stretched
* aligned cleanly with the logo mark
* visually stronger than it is now

### 3. Widen the left brand/header block

The entire left branding section in the header should be given more room.

Please widen that section so the larger logo mark and wordmark fit comfortably.

Adjust if needed:

* brand area width
* spacing between logo mark and wordmark
* spacing between wordmark and divider
* spacing before the project name block

Do **not** make it cramped.

### 4. Keep the project name area clean

After resizing the branding, the divider, project name, and repo path should still align nicely.

Preserve the general structure:

```text
[logo mark] [wordmark] | [project name]
                        [repo path]
```

## Important restraint

Do not:

* shrink it back down
* make only a tiny increase
* distort the imported assets
* crop the logo mark
* blur the wordmark
* add any extra text
* change the rest of the UI

This pass should be a **clear, visible increase**.

Not a subtle tweak.

## Acceptance criteria

After this pass:

1. The logo mark is about **2x the current rendered size**.
2. The wordmark is about **2x the current rendered size**.
3. The left header brand area is wider to support the larger assets.
4. The assets remain crisp and undistorted.
5. The divider, project name, and repo path still align cleanly.
6. The branding finally feels intentional and readable.
7. No unrelated UI or functionality changes are made.
