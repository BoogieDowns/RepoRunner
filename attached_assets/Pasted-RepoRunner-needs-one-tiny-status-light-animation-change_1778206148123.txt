RepoRunner needs one **tiny status-light animation change** only.

Do **not** redesign the UI.
Do **not** change layout.
Do **not** change functionality.
Do **not** change service logic, process management, IPC, setup behavior, logs behavior, meteors, buttons, or product flow.

This is only about removing pulse behavior from the engine/status lights.

## Requested change

I no longer want any of the status lights to pulse.

Instead, when a light is active, it should **illuminate and stay illuminated**.

## Remove pulsing from

Remove pulse animation from:

```text
RUNNING
STARTING
STOPPING
```

Do not animate them with a repeating pulse.

## Desired behavior

### RUNNING

The red light should:

* illuminate red
* stay steadily lit
* keep its plastic lens look
* keep a subtle steady glow
* not pulse

### STARTING / STOPPING

The peach light should:

* illuminate peach
* stay steadily lit
* keep its plastic lens look
* keep a subtle steady glow
* not pulse

### STOPPED

The stopped light should remain:

* unlit
* dark/smoked
* static

## Timing clarification

If there is currently an illumination transition when the status changes, keep that transition feeling smooth and roughly as quick as it is now.

But after the light reaches its illuminated state, it should remain steady.

So:

```text
state changes → light turns on smoothly → light stays on
```

Not:

```text
state changes → light repeatedly pulses
```

## Preserve styling

Keep:

* current plastic/ribbed lens styling
* current square indicator shape
* current red running color
* current peach starting/stopping color
* current stopped unlit styling
* current text inside the light
* current layout

## Remove / disable animations

Please remove or disable any repeating animation classes/keyframes for:

* red running pulse
* peach starting/stopping pulse

Do not leave unused pulse classes applied to active indicators.

## Acceptance criteria

After this change:

1. `RUNNING` light is steadily illuminated red and does not pulse.
2. `STARTING` light is steadily illuminated peach and does not pulse.
3. `STOPPING` light is steadily illuminated peach and does not pulse.
4. `STOPPED` remains unlit/static.
5. Status change transitions can remain smooth, but no repeating pulse remains.
6. No unrelated UI or functionality changes are made.
