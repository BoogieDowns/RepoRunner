RepoRunner needs one **final Quick Actions button readability polish pass** only.

Do **not** redesign the UI.
Do **not** change layout.
Do **not** change button order.
Do **not** change button labels.
Do **not** change icon shapes.
Do **not** redraw any icons.
Do **not** change functionality.
Do **not** change engine lights, logs, meteors, setup modal, header, IPC, process logic, or product flow.

This is only about making the existing Quick Actions buttons read a little cleaner and feel more premium.

## Current state

The icon alignment pass helped.

The fixed 18×18 icon slot and label wrapper are good. Keep those.

Do not undo them.

But the button row still needs a small visual readability polish.

## Main issues

1. The grey/inactive buttons still feel slightly too dim.
2. The bottom row feels slightly flatter than the red action row.
3. Icon/text spacing is technically aligned, but could use a tiny bit more breathing room.
4. The buttons could use slightly clearer glass depth without changing the design.

## Requested changes

### 1. Slightly increase grey button readability

For the neutral grey buttons:

```text
Pull
Install
Restart
Open
```

Increase clarity slightly.

Good options:

* slightly brighten label/icon color
* slightly improve contrast
* slightly lift the internal highlight
* slightly increase glass reflection visibility

Target:

```text
5–10% more readable
```

Do not make them bright white.
Do not make them look active/red.
Just make them easier to read.

### 2. Improve neutral button glass depth

The grey buttons should feel a little more like the same premium glass system as the red buttons.

Subtly enhance:

* inner highlight
* lower shadow
* reflective sheen
* bevel definition

Do not overdo it.
Do not make them chrome.
Do not make them cartoon shiny.

### 3. Add tiny icon/text breathing room

Increase the gap between icon and label by a tiny amount if needed.

Suggested:

```text
+1px to +2px
```

Only if it improves the visual feel.

The content should still stay centered inside each button.

### 4. Preserve icon geometry

Do not change the actual icon SVG shapes.

Do not modify:

* Pull icon geometry
* Install icon geometry
* Restart icon geometry
* Open icon geometry
* Stop icon geometry
* Start play icon geometry

Only adjust shared rendering/spacing/color if needed.

### 5. Preserve disabled state

The disabled Stop Engine and disabled Start buttons should still clearly read as disabled.

Do not make disabled buttons look clickable.

If increasing general contrast affects disabled buttons, keep disabled opacity controlled separately.

## Keep

Keep:

* fixed 18×18 icon slot
* label wrapper alignment
* current two-row layout
* current button sizes
* current LunaObscura font
* current icon set
* current red/grey color logic

## Acceptance criteria

After this pass:

1. Grey buttons are slightly easier to read.
2. Bottom row feels more premium and less flat.
3. Icon/text spacing feels clean and not cramped.
4. Icons remain aligned in the 18×18 slot.
5. Icon shapes are not redrawn.
6. Disabled states still look disabled.
7. No unrelated UI or functionality changes are made.
