RepoRunner needs one **strict engine light shape correction** only.

Do **not** redesign the UI.
Do **not** change layout.
Do **not** change engine/service logic.
Do **not** change timings.
Do **not** change illumination behavior.
Do **not** change the plastic light styling direction.
Do **not** change button styling, logs, meteors, setup modal, or header.

This is only about fixing the **stopped-state engine light shape/size**.

## Problem

The stopped light is now showing the correct text behavior (blank), but the **physical light itself has changed size/shape**.

When stopped, the light is now:

- narrower
- visually smaller
- a different shape/proportion

That is wrong.

The stopped light should use the **exact same physical light shell** as the running light.

## Required correction

Make the stopped engine light use the **same exact dimensions, same shape, same border radius, same padding, same lens styling, and same container structure** as the running light.

The only difference between states should be:

- **RUNNING / active states** → illuminated red light with `RUNNING` text
- **STOPPED state** → same light, same size, same shape, same shell, but dark/unlit and **blank**
- **STARTING / STOPPING** → same light, same size, same shape, same shell, illuminated/fading behavior preserved, text behavior unchanged from current simplified system

## Important

Do **not** render a different “small/off” variant for STOPPED.

Do **not** let the stopped state use a different:

- width
- height
- min-width
- padding
- border radius
- font box
- display mode
- lens shell
- ribbed texture
- bevel/inset structure
- border thickness
- glow container

The stopped light should be the **same component frame** as the running light, just dark and blank.

## Implementation guidance

Please inspect the current engine light component and make sure:

1. The stopped state uses the **same outer wrapper** as the running state.
2. The stopped state uses the **same inner lens element** as the running state.
3. The stopped state does **not** swap to a smaller badge/pill variant.
4. The stopped state keeps the same width and height as the running state.
5. If text removal caused the box to shrink, preserve sizing with an explicit width / min-width / height and center alignment.
6. If necessary, keep the text element present but invisible in STOPPED state (`opacity: 0` or equivalent), rather than removing the layout structure entirely.
7. Preserve the current ribbed/plastic red-light styling system.

## Visual rule

Think of it like this:

- `RUNNING` = bright red illuminated plastic light
- `STOPPED` = the exact same plastic light, but powered off and blank

Not a different badge.
Not a smaller light.
Not a narrower shape.

## Acceptance criteria

After this change:

1. The stopped light is blank.
2. The stopped light remains dark/unlit.
3. The stopped light is the **exact same size and shape** as the running light.
4. Running and stopped lights share the same shell/lens structure.
5. No layout shift occurs between states.
6. No unrelated UI or functionality changes are made.

Please make this a **strict visual correction pass only**.