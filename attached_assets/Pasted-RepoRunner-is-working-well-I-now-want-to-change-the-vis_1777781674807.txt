RepoRunner is working well. I now want to change the **visual theme only** so it matches the dark green color scheme shown in the reference screenshot.

Do **not** redesign the app.
Do **not** change layout significantly.
Do **not** change app behavior.
Do **not** change process management, IPC, or functionality.

This is a **theme / styling pass only**.

## Goal

Restyle RepoRunner so it uses a **dark green / matrix-inspired theme** similar to the attached reference.

The app should feel:

* dark
* sleek
* slightly hacker / terminal-inspired
* clean
* modern
* readable
* restrained, not gimmicky

It should still feel like a polished desktop product, not a novelty theme.

## Important direction

Use the reference image as inspiration for the color system.

The key feel is:

* near-black background
* dark green-tinted surfaces
* neon/mint green accent
* muted green foreground text
* subtle green borders/glow
* low-saturation, cohesive contrast

Do **not** make everything bright green.
The theme should be controlled and tasteful.

## Color direction

Please shift the app toward a palette approximately like this:

* Main background: `#040805`
* Elevated dark surface: `#0b120f`
* Card/panel surface: `#0f1713`
* Subtle border: `#173126`
* Muted divider/border: `#1c3a2c`
* Primary accent green: `#1EFF5A`
* Softer accent green: `#27d96b`
* Pale foreground green/off-white: `#B8FFCA`
* Secondary text: `#7FA18B`
* Muted text: `#5D7465`

You do **not** need to match these perfectly, but the theme should clearly move in this direction.

## UI theme changes

### 1. Global app theme

Apply the green theme across:

* app background
* header
* cards
* buttons
* inputs
* logs panel
* borders
* status pills

The app should look cohesive from top to bottom.

### 2. Header styling

Keep the current header layout, but restyle it to match the new theme.

Please adjust:

* RepoRunner logo background/border
* wordmark color
* project name color
* repo path color
* Edit Setup styling

The header should feel integrated with the green theme, not just black/gray.

### 3. Cards and surfaces

All cards/panels should have:

* dark green-tinted surfaces
* subtle green border treatment
* slightly lifted appearance
* restrained depth

Do not overdo glow or blur.
A very subtle premium glow is okay if tasteful.

### 4. Buttons

Restyle buttons to match the new palette.

Guidance:

* Primary buttons: green-accent treatment
* Secondary buttons: dark surface with subtle green border/text
* Danger button: keep red, but refine it so it still fits the theme
* Disabled buttons: subdued and clearly inactive
* Hover states: subtle and polished

Keep button labels the same.

### 5. Service status pills

Restyle status pills to fit the theme.

Use:

* Running = green
* Starting / Stopping = amber-green / warm yellow-green
* Stopped = muted dark neutral
* Failed = restrained red
* Unknown = muted gray-green

Keep them readable and clean.

### 6. Logs panel

Restyle the logs panel so it fits the green theme.

Please keep:

* monospace font
* readable timestamps
* readable source labels
* clear Copy / Clear controls

Suggested direction:

* very dark background
* green-tinted text accents
* muted timestamps
* source badges that still differentiate system/frontend/backend clearly

Do not make the logs hard to read.

### 7. Setup screen

Apply the same green theme to the setup panel.

Restyle:

* panel surface
* inputs
* labels
* helper text
* Choose Folder button
* Save Configuration button
* close X button

The setup screen should match the dashboard visually.

### 8. Typography and contrast

Keep readability high.

Important:

* do not use ultra-bright green for all text
* major text should stay readable and comfortable
* use pale green/off-white for primary text
* use muted green-gray for secondary text
* use neon green mainly as an accent, not the default text color

## Suggested implementation approach

If helpful, centralize the theme with CSS variables / theme tokens.

For example:

```css
:root {
  --bg: #040805;
  --surface: #0f1713;
  --surface-elevated: #131d18;
  --border: #173126;
  --border-strong: #1f4130;
  --text: #b8ffca;
  --text-secondary: #7fa18b;
  --text-muted: #5d7465;
  --accent: #1eff5a;
  --accent-soft: #27d96b;
  --danger: #c94b57;
  --warning: #c6a84a;
}
```

You do not have to use exactly these names, but a centralized theme system is preferred.

## Strict constraints

Do not change:

* layout structure
* workflow
* setup fields
* command behavior
* logs behavior
* service lifecycle
* restart logic
* app architecture

Do not add:

* new features
* new sections
* charts
* analytics
* terminal tabs
* IDE features
* GitHub login
* branch switching
* multi-project support

## Acceptance criteria

After this pass:

1. RepoRunner clearly uses the dark green matrix-like theme from the reference direction.
2. The UI feels cohesive and more branded.
3. The app remains easy to read.
4. The current layout is preserved.
5. The setup screen matches the dashboard.
6. Buttons, cards, logs, and header all reflect the new theme.
7. No functionality is broken.

Please keep the result tasteful, restrained, and polished.
