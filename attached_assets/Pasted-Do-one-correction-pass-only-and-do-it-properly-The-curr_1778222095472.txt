Do one correction pass only, and do it properly.

The current OPEN icon is wrong.

It does NOT match the pasted reference closely enough.
Do not freestyle.
Do not loosely interpret it.
Do not swap in a generic “open”, “launch”, “link”, or “external” icon.

Do not redesign the UI.
Do not change layout.
Do not change button sizes.
Do not change button text.
Do not change fonts.
Do not change glass styling.
Do not change spacing for other buttons.
Do not touch any icon except OPEN.

This task is ONLY:
Fix the OPEN icon so it matches the pasted reference image closely and clearly.

## Source of truth

Use the pasted reference image as the visual source of truth.

Do not simplify it into some other symbol.
Do not replace it with a normal arrow or external-link icon.
I want a close visual match.

## What the OPEN icon must look like

The icon should look like:

- one clean circular ring near the upper-right area of the icon
- two parallel diagonal trailing lines extending down-left from the circle
- the two lines should be separate and clearly visible
- the circle should be the dominant head of the symbol
- the whole symbol should feel like a sleek technical glyph

Very important:
- there should NOT be an arrowhead
- there should NOT be a box-with-arrow icon
- there should NOT be a chain/link icon
- there should NOT be a magnifying glass look
- there should NOT be a random abstract line symbol

It should read like the reference:
a circle with two parallel trailing lines.

## Build method

Create it as a custom SVG icon.

Requirements:
- use stroke only
- use `currentColor`
- rounded line caps
- rounded line joins
- no fill
- crisp geometry
- centered in the icon box

The best construction is likely:
- 1 stroked circle
- 2 separate diagonal line segments

## Visual proportion requirements

Match the reference proportions closely:

- circle slightly right of center
- two trailing lines angled down-left
- top trailing line connects or nearly connects to the circle
- lower trailing line sits below it and runs parallel
- both lines should be long enough to read clearly at small size

Do not make the circle too small.
Do not make the lines too short.
Do not compress the icon into a tiny unreadable shape.

## Visual sizing requirements

The icon must be:

- clearly visible
- not tiny
- not faint
- not overly thin
- optically centered
- visually balanced with the other button icons

Target:
- icon visual area roughly 14–16px wide inside the standard button icon box
- stroke weight approximately 1.8–2.1px, whatever best matches the other icons
- icon/text spacing should remain tidy and consistent with the other buttons

## Hard acceptance criteria

Do not stop at “technically updated.”
Visually compare the result to the pasted reference before finishing.

The final OPEN icon must satisfy all of these:

1. It clearly matches the pasted reference image.
2. It has one clear circular head.
3. It has two clear parallel trailing lines.
4. It does not look like an arrow, chain, box-arrow, or generic external-link icon.
5. It is readable at button size.
6. It matches the visual weight of the other button icons.
7. No unrelated UI changes are made.

## Important instruction

If the first SVG attempt does not visually match the reference, revise it immediately in the same pass before finishing.

Do not leave me with a half-correct icon.