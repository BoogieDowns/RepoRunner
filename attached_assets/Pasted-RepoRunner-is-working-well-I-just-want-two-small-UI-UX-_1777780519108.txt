RepoRunner is working well. I just want two small UI/UX fixes.

Do not redesign the app.
Do not change the app architecture.
Do not change process management or service lifecycle logic.
Do not add any new product features.

## Fix 1: Remove duplicate header status pills

In the top-right of the header, there are currently two status pills showing service states (for example, Stopped / Stopped, or Starting / Running).

Please remove those **header-level status pills** entirely.

Important:

* Keep the **Services card in the main dashboard content area**.
* Keep the **Frontend / Backend status pills in that Services card**.
* Only remove the duplicate status indicators in the top-right header.

The top-right header area should just contain:

* Edit Setup
* any existing small settings/edit icon if already present

It should **not** contain duplicate service status pills.

## Fix 2: Add a close X to the Setup RepoRunner panel

In the Setup RepoRunner screen/panel/card, add a small **X close button** in the **top-right corner** of the setup box.

Behavior:

* Clicking the X should close the setup panel.
* If the setup panel was opened from **Edit Setup**, closing it should return the user to the main dashboard without saving changes.
* Do not auto-save on close.
* Existing Save Configuration behavior should stay exactly the same.

UX requirements:

* The X should be subtle, clean, and visually consistent with the dark UI.
* It should be clearly clickable.
* A hover state would be nice.
* It should not look oversized or distracting.

## Functional safety

Do not break any working behavior, including:

* setup and save config
* edit setup
* pull latest
* install
* start frontend
* start backend
* stop services
* restart all
* open preview
* logs
* copy logs
* clear logs
* service status updates

## Acceptance criteria

After this change:

1. The duplicate service status pills are gone from the top-right header.
2. The service status pills remain in the Services card in the main content area.
3. The Setup RepoRunner panel has an X button in the top-right corner.
4. Clicking the X closes the setup panel and returns to the main dashboard.
5. Closing with X does not auto-save changes.
6. Save Configuration still works exactly as before.
7. No existing functionality is broken.
