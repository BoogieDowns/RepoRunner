Stop tweaking this by eye. The top-left header logo layout is still wrong.

Please do one **exact header layout correction** only.

Do **not** redesign the app.
Do **not** change functionality.
Do **not** change dashboard cards, buttons, engine lights, logs, meteors, setup modal, IPC, process logic, or product flow.
Do **not** bring back the wordmark.

This is only about fixing the **top-left header logo + project name layout** with exact sizing.

## Current problem

The RR logo is still wrong because the header brand area is not being laid out correctly.

It currently looks too small, disconnected, and poorly aligned.

Do not keep randomly resizing the image.

Fix the header structure and sizing properly.

---

## Required exact layout

The top-left header should be one horizontal flex row:

```text
[ RR logo block ] [ divider ] [ project text block ]
```

Use exact, controlled sizing.

---

## 1. Header height

Set the app header/top bar height to approximately:

```text
72px
```

If the existing header is already close, keep it close.

Do not let the logo float inside a huge empty area.

---

## 2. RR logo size

Set the RR logo image to an explicit size:

```text
width: 72px
height: auto
max-height: 42px
```

The logo should render around **72px wide**, not tiny.

It should be visibly larger than it is now, but not massive.

Do not use vague percentages.
Do not use auto sizing that lets the asset collapse small.
Do not let parent containers shrink it.

---

## 3. Logo block

Wrap the logo in a fixed-size brand/logo block:

```text
width: 96px
height: 72px
display: flex
align-items: center
justify-content: center
```

This gives the logo a stable home.

The logo should sit centered inside this block.

---

## 4. Divider

Place the divider immediately after the logo block.

Use:

```text
height: 28px
margin-left: 8px
margin-right: 20px
```

The divider should sit vertically centered.

It should visually separate the logo from the project block.

---

## 5. Project text block

The project name/path block should sit directly after the divider.

Use flex column:

```text
display: flex
flex-direction: column
justify-content: center
```

Project name and path should remain aligned and readable.

Do not move the project block far away from the logo.

---

## 6. Full brand group

The whole left header group should use:

```text
display: flex
align-items: center
height: 72px
padding-left: 20px
```

The logo, divider, and project text should all align on the same vertical center.

---

## 7. Remove conflicting CSS

Inspect and remove/override any CSS that causes the logo to stay tiny, including:

* max-width too small
* max-height too small
* transform scale
* object-fit rules that shrink it
* parent width constraints
* flex shrink
* old brand/wordmark sizing rules
* inherited image sizing from the removed wordmark

Set:

```text
flex-shrink: 0
```

on the logo block and logo image if needed.

---

## Final visual target

The header should look like:

```text
[ large RR logo ] | my-app
                  /Users/you/projects/my-app
```

The RR logo should be a strong visible brand mark.

It should not be tiny.
It should not float.
It should not be randomly positioned.
It should not be disconnected from the project block.

---

## Acceptance criteria

Before finishing, visually verify:

1. The wordmark is gone.
2. Only the RR logo remains.
3. The RR logo is about 72px wide.
4. The logo sits inside a 96px-wide logo block.
5. The header is about 72px tall.
6. The logo, divider, and project text are vertically centered.
7. The project text sits close to the divider, not floating far away.
8. The logo is not tiny.
9. The logo is not massive.
10. No unrelated UI or functionality changes are made.

If the logo still looks tiny after this, the task is not complete.
Find the CSS constraint shrinking it and fix that constraint.
