The previous branding pass missed the mark.

Please do one **focused correction pass** on the top-left branding only.

Do **not** redesign the app.
Do **not** change layout outside the brand area.
Do **not** change functionality.
Do **not** change dashboard, buttons, services, logs, setup modal, meteor effects, IPC, or product flow.

This is only about correcting the **top-left logo + wordmark**.

## First: remove the unwanted extra label

Remove the small text:

```text
DEV CONSOLE
```

I do **not** want that under the wordmark.

The brand area should contain only:

* the logo mark
* the `REPORUNNER` wordmark
* the divider
* the project name / repo path

Nothing extra.

## 1. Use the actual LunaObscura font for the wordmark

I have added the font file locally to the Replit project.

The file name is:

```text
hs_lunaobscura Font
```

Please find this file in the project file tree and wire it up properly with `@font-face`.

Use this font **only** for the top-left `REPORUNNER` wordmark.

Do not use it for:

* body text
* logs
* form labels
* buttons
* service labels
* project name
* repo path

The wordmark should read:

```text
REPORUNNER
```

and feel:

* sharper
* more premium
* more sci-fi
* more intentional
* less generic

Please tune:

* size
* tracking
* vertical alignment
* spacing relative to the logo

If the exact file extension needs to be referenced, inspect the file tree and use the actual full file path/name. Do not guess if the file has an extension such as `.ttf`, `.otf`, or `.woff`.

## 2. Replace the current logo with a better RR monogram

The current logo is not good enough.

Please replace it with a stronger custom monogram.

### Desired concept

A compact **double-R / RR monogram** with:

* two stylized Rs
* mirrored or back-to-back
* a shared central vertical stem if helpful
* geometric construction
* angular lower legs
* clean linework
* strong small-size readability

Think:

* compact sci-fi monogram
* premium app icon
* technical / industrial
* dark red console aesthetic

Do not make it:

* generic
* overly simple
* decorative
* sports-team-like
* fantasy-like
* too thin
* too intricate

## 3. Fix logo sizing and container balance

The current icon looks off in size and balance.

Please make sure the new monogram:

* is optically centered
* fills the icon container better
* has better scale
* has proper padding
* feels balanced next to the wordmark

The icon container can remain a compact square with:

* dark background
* subtle red border or edge glow
* restrained red accent

## 4. Keep the header clean

Do not add any new sublabels, taglines, or microcopy.

Do not add:

* DEV CONSOLE
* status subtitles
* extra branding copy

Keep the header branding clean and minimal.

## Acceptance criteria

After this change:

1. `DEV CONSOLE` is removed.
2. The local `hs_lunaobscura Font` file is used for the `REPORUNNER` wordmark.
3. The wordmark feels more distinctive and less generic.
4. The logo is replaced with a stronger RR monogram.
5. The logo is better sized and balanced in its container.
6. The top-left branding feels more premium and intentional.
7. No unrelated UI or functionality changes are made.
