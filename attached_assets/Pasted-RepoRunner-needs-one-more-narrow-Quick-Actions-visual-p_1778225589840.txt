RepoRunner needs one more **narrow Quick Actions visual polish pass** only.

Do **not** redesign the UI.
Do **not** change layout.
Do **not** change button order.
Do **not** change button labels.
Do **not** change icon SVG geometry.
Do **not** change icon size.
Do **not** change functionality.
Do **not** change engine lights, logs, meteors, setup modal, header, IPC, process logic, or product flow.

This is only about the final visual finish of the existing Quick Actions buttons.

## Current state

The last pass improved:

* rim catchlight softness
* lower-body shadow
* hover material consistency
* active/pressed physical feel

Good direction.

Now do a final refinement pass focused on **micro contrast, edge cleanliness, and button group cohesion**.

Do not add more major effects.

---

## 1. Check button readability at normal zoom

Look at the actual preview size.

Make sure every button label and icon is comfortably readable:

```text
START FRONTEND
START BACKEND
STOP ENGINE
PULL
INSTALL
RESTART
OPEN
```

If anything feels slightly too dim or buried inside the glass, make a tiny contrast correction.

Keep it subtle.

Do not make the labels bright white.

---

## 2. Clean up edge sharpness

The buttons should feel crisp against the dark panel.

Please refine only if needed:

* outer border clarity
* bottom edge definition
* side bevel balance
* corner edge cleanliness

Avoid making the border thick or obvious.

The edge should feel precise, not outlined.

---

## 3. Balance red and neutral button materials

The red and neutral buttons should feel like the same physical button family.

Check that:

* red buttons do not look much louder than neutral buttons
* neutral buttons do not look flatter than red buttons
* both share similar depth, gloss, and edge treatment
* disabled buttons still sit back correctly

Make tiny adjustments only.

---

## 4. Check disabled state carefully

Disabled buttons should still look inactive, but not broken.

Make sure disabled buttons:

* are dim enough to read as disabled
* still preserve the button material form
* do not disappear completely
* do not look clickable

Do not change disabled behavior.

---

## 5. Reduce any CSS-gradient feeling

If any button still looks like a flat CSS gradient rather than glass, refine the layering slightly.

Focus on:

* smoother transitions
* less obvious bands
* better blend between highlight, body, and lower shadow

Do not increase brightness just for the sake of it.

---

## 6. Do not touch icon shapes

The icons are good enough for now.

Do not change their SVG paths.
Do not change their size.
Do not redraw the Open icon.
Do not redraw Restart.

Only adjust icon color/opacity if absolutely needed for readability, and keep it shared/consistent.

---

## 7. Final panel-level check

Visually inspect the full Quick Actions card.

It should feel:

* finished
* cohesive
* premium
* readable
* balanced
* like a physical sci-fi control panel

Not:

* overly bright
* muddy
* flat
* mismatched
* overworked

## Acceptance criteria

After this pass:

1. All button labels and icons are readable at normal preview size.
2. Button edges feel crisp but not over-outlined.
3. Red and neutral buttons feel like one cohesive material system.
4. Disabled buttons remain clearly inactive.
5. The glass effect feels smoother and less CSS-gradient-like.
6. No icon geometry, layout, labels, or functionality are changed.
