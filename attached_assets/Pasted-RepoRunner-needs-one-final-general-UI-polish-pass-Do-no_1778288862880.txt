RepoRunner needs one **final general UI polish pass**.

Do **not** redesign the app.
Do **not** change functionality.
Do **not** change button labels.
Do **not** change button order.
Do **not** change service logic, IPC, process management, setup behavior, logs behavior, or product flow.

This is a **visual polish pass only**.

## Current state

The app is looking really good now.

The main UI direction is working:

* dark sci-fi control panel
* crimson glass action buttons
* smoked-glass neutral buttons
* RR logo header
* red plastic engine lights
* terminal logs
* meteor/ripple background effects

The goal now is not to redesign anything.

The goal is to inspect the full app and make small final refinements where something clearly looks unfinished.

## Important instruction

Inspect the live preview first.

Check the app as a whole before editing.

Do not make changes just to make changes.

Only adjust things that are visibly off.

## Areas to inspect

### 1. Header

Check:

* RR logo size and alignment
* divider alignment
* project name size/readability
* repo path readability
* Edit Setup alignment and contrast

Make tiny corrections only if needed.

### 2. Quick Actions card

Check:

* button alignment
* icon/text clarity
* red button crimson consistency
* neutral button smoked-glass finish
* disabled state readability
* card border/background balance

Do not change button labels, order, or layout.

### 3. Engine card

Check:

* ENGINE label readability
* Frontend/Backend labels
* port text readability
* red plastic light alignment
* red light size consistency
* card border/background balance

Do not change light behavior or service logic.

### 4. Logs panel

Check:

* APP LOGS label
* count badge
* Copy/Clear controls
* log text readability
* source label colors
* row spacing
* scrollbar appearance
* empty state if visible

Do not change log behavior.

### 5. Setup modal

Check:

* RepoRunner Setup heading
* field alignment
* Preview URL wide row
* command fields row
* port fields row
* input readability
* close button
* Save Configuration button
* modal border/shadow/scrim

Do not change setup functionality.

### 6. Background effects

Check:

* meteor visibility
* red ripple effect
* background noise/grain
* panel readability over effects
* no distracting visual artifacts

Do not change behavior unless there is a clear visual issue.

## Polish rules

Only make small refinements such as:

* tiny contrast adjustments
* tiny alignment fixes
* tiny spacing corrections
* subtle border/shadow tuning
* small readability improvements

Do **not**:

* redesign sections
* refactor working systems
* change component structure unnecessarily
* alter functionality
* introduce new features
* change layout dramatically
* change the visual direction

## Preserve

Preserve:

* current dark sci-fi style
* current crimson brand color
* current button system
* current custom icons
* current engine light behavior
* current logs color system
* current setup modal layout
* current meteor/ripple behavior

## Acceptance criteria

After this pass:

1. The app looks slightly more finished and cohesive.
2. No completed area is made worse.
3. No major visual direction changes are made.
4. No functionality changes are introduced.
5. Any code change is tied to a visible polish issue.
6. If everything already looks good, make no unnecessary changes and report that the UI is ready.
