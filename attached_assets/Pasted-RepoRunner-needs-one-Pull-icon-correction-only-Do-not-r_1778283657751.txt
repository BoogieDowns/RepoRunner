RepoRunner needs one **Pull icon correction** only.

Do **not** redesign the UI.
Do **not** change layout.
Do **not** change button sizes.
Do **not** change labels.
Do **not** change typography.
Do **not** change glass styling.
Do **not** change any other icons.
Do **not** change functionality.

This task is only about correcting the `Pull` button icon.

## Current issue

Over multiple iterations, the `Pull` icon has drifted away from the intended reference.

Stop tweaking the current icon loosely.

Use the new reference image I am pasting and correct the `Pull` icon so it matches that reference closely.

## What to do

Replace or redraw the `Pull` icon using a fresh custom SVG if needed.

The new Pull icon must:

* match the pasted reference closely
* be simple and clean
* read clearly at button size
* look polished
* be optically centered
* use `currentColor`
* use the same stroke weight as the other button icons
* feel like it came from the same factory as the rest of the icon set

## Important restraint

Do not interpret this loosely.

Do not make a “version inspired by” the reference.

Do not keep the current icon if it does not match properly.

If necessary, fully replace the existing Pull icon geometry.

## Consistency requirements

The corrected Pull icon must visually match the icon system used by:

* Start Frontend
* Start Backend
* Stop Engine
* Install
* Restart
* Open

It should feel consistent in:

* size
* line thickness
* centering
* visual weight
* cleanliness of shape

## Scope

Only change the `Pull` icon.

Do not touch:

* Start Frontend icon
* Start Backend icon
* Stop Engine icon
* Install icon
* Restart icon
* Open icon
* button text
* button layout
* button spacing
* card layout
* engine panel
* logs
* meteors
* modal
* any other UI

## Acceptance criteria

1. The Pull icon is corrected to closely match the pasted reference.
2. The icon is clean and readable at button size.
3. The icon is visually consistent with the rest of the button icon set.
4. The icon is centered and polished.
5. No unrelated UI or functionality changes are made.
