RepoRunner looks close, but the Quick Actions spacing still needs a precise cleanup pass.

Do not redesign the UI.
Do not change functionality.
Do not change button styling direction.
Do not change the engine panel, logs, setup modal, meteors, or product flow.

This is only a layout / spacing fix for the Quick Actions button area.

## Problem

The current Quick Actions buttons are too cramped and the spacing/composition feels off.

Issues visible now:
- buttons feel jammed together
- row spacing is awkward
- the first row does not breathe enough
- the overall button group does not feel balanced
- some buttons feel visually crowded near each other
- the arrangement looks slightly messy instead of clean and intentional

## Goal

Make the Quick Actions section feel:
- clean
- balanced
- evenly spaced
- visually structured
- premium
- easy to scan

## Required fix

Use a more explicit layout for the Quick Actions buttons.

### Keep this exact button grouping:

Row 1:
- Pull Latest
- Install
- Start Frontend
- Start Backend

Row 2:
- Stop Engine
- Restart All
- Open Preview

## Spacing requirements

### 1. Improve horizontal spacing
Add more consistent horizontal gap between buttons.
Nothing should feel crammed.

Suggested direction:
- slightly more space between neighboring buttons
- keep spacing even, not random
- avoid any near-touching visual feel

### 2. Improve vertical spacing
Increase the space between row 1 and row 2 slightly so the two rows breathe better.

### 3. Balance the group visually
The second row should feel intentionally placed under the first row, not like a leftover wrap.

Use one of these approaches:
- center the second row relative to the first row
or
- align it cleanly to a deliberate internal grid

Either is fine, as long as it looks intentional and visually pleasing.

### 4. Prevent clipping / cramped widths
Make sure all button labels fit comfortably and the row has enough room.

If needed:
- slightly adjust button widths
- slightly adjust the internal layout of the Quick Actions panel
- slightly reduce or normalize width differences between buttons

But do not make the panel look oversized or sloppy.

### 5. Use a proper layout system
If not already, switch this button area to an explicit layout:
- CSS grid
or
- a structured flex column with two row wrappers

Do not rely on a loose wrap that causes awkward spacing.

## Important restraint

Do not change:
- button text
- button icons
- glass / glossy styling
- font choice
- colors
- hover states
- click behavior
- disabled logic
- app layout outside this button group

## Acceptance criteria

After this pass:
1. The Quick Actions buttons have cleaner horizontal spacing.
2. The spacing between the two rows is improved.
3. The rows feel balanced and intentionally arranged.
4. No button feels cramped or awkwardly placed.
5. The layout looks more polished and visually pleasing.
6. No unrelated UI or functionality changes are made.